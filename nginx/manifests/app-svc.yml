---
apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: nginx-ns
  labels:
    app: app
    tier: backend
spec:
  selector:
    app: app
    tier: backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP

